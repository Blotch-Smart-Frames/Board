# Copilot Instructions

## Environment

- We may be behind a corporate firewall — `npm install`, `npm ci`, and any other package-fetching commands can hang or fail intermittently. Do NOT run them yourself; ask the user to run installs and share the result.
