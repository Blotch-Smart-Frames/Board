// Copy this file to `environment.ts` and fill in real values from the Firebase
// console / Google Cloud console. `environment.ts` is gitignored — it is never
// committed. These are the same values the source React app read from `.env`
// (VITE_FIREBASE_*, VITE_GOOGLE_CLIENT_ID).
export const environment = {
  firebase: {
    apiKey: '',
    authDomain: '',
    projectId: '',
    storageBucket: '',
    messagingSenderId: '',
    appId: '',
  },
  google: {
    clientId: '',
  },
};
