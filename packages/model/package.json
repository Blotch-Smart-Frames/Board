{
  "name": "@blotch/model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared domain models for the board app (client + functions)",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "main": "./src/index.ts",
  "scripts": {
    "cspell": "cspell .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
